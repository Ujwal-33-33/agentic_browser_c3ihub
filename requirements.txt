# Secure Agentic Browser - Dependencies
# ======================================

# Core browser automation
playwright>=1.40.0

# OpenAI-compatible client (for Ollama)
openai>=1.0.0

# Environment variable management
python-dotenv>=1.0.0

# HTML parsing and security
beautifulsoup4>=4.12.0
lxml>=4.9.0

# String matching for deceptive UI detection
# Using standard library difflib (no external dep needed)
# Optional: python-Levenshtein for faster fuzzy matching
python-Levenshtein>=0.21.0

# Terminal colors for security alerts
termcolor>=2.3.0

# Web UI (optional)
streamlit>=1.28.0
